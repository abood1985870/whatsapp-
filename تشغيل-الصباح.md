# تشغيل النظام صباح العرض — خطوة بخطوة

> الوقت المتوقع: 5 دقائق. نفّذ الخطوات بالترتيب.

## 1. شغّل Docker Desktop

- افتحه من قائمة ابدأ.
- انتظر حتى تستقر أيقونة الحوت (تتوقف عن التحرك).

## 2. شغّل الخدمات الأساسية

افتح PowerShell **واحد فقط** ونفّذ:

```powershell
cd c:\Users\abood\Desktop\qanoai-whatsappsupport
pnpm run docker:up
```

انتظر حتى ينتهي الأمر (قاعدة البيانات + Redis + الواتساب + التخزين).

## 3. شغّل التطبيق

في نفس النافذة:

```powershell
pnpm dev
```

انتظر نصف دقيقة حتى تهدأ الرسائل.

> ⚠️ **مهم**: نافذة طرفية واحدة فقط. لا تشغّل `pnpm dev` مرتين — هذا سبب مشاكل الأمس.

## 4. اربط الواتساب

- افتح المتصفح: http://localhost:3000
- سجّل الدخول.
- اذهب لصفحة **القنوات** → امسح رمز **QR** من واتساب جوالك
  (واتساب → الإعدادات → الأجهزة المرتبطة → ربط جهاز).

## 5. بروفة قبل العرض (لا تتجاوزها)

1. من صفحة **وكلاء الذكاء الاصطناعي** → لوحة الاختبار الحي → اكتب سؤالًا بالعربية → يجب أن يرد.
2. أرسل رسالة واتساب حقيقية من جوال آخر → يجب أن يصل رد آلي خلال 5-10 ثوانٍ.

## إذا شيء ما اشتغل

- تأكد أن Docker شغال (الخطوة 1).
- أغلق نافذة الطرفية وأعد الخطوات 2 و 3.
- الذكاء الاصطناعي يعمل بمفتاح Gemini — **لا تغيّر إعدادات النماذج**.
